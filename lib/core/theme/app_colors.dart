import 'package:flutter/material.dart';

class AppColors {
  // Light mode
  static const lightPrimary = Color(0xFF155dfc);
  static const lightSecondary = Color(0xFF3472fc);
  static const lightBackground = Color(0xFFFFFFFF);
  static const lightTextPrimary = Colors.black87;
  static const lightTextSecondary = Colors.grey;

  // Dark mode
  static const darkPrimary = Color(0xFF2563EB);
  static const darkSecondary = Color(0xFF3472fc);
  static const darkBackground = Color(0xFF000000);
  static const darkTextPrimary = Colors.white;
  static const darkTextSecondary = Colors.white70;
}
